# Cleanup handoff summary

Use this compact context for the next session:

- Repository is now centered on `prompts/00-*` plus canonical bilingual role prompts `010-140`.
- Legacy prompt files, old prompt branch expectations, old helper folders, and old story template files were removed or replaced.
- `README.md`, `AGENTS.md`, `docs/story-project-structure.md`, `prompts/00-workflow.md`, and `docs/role-map.md` are aligned to bilingual prompt naming.
- `stories/_template` is now canonical and includes handoff placeholders for roles `010-140`.
- Concrete stories now belong under ignored `private/stories/<story-slug>/`.
- Existing private story workspace has completed content through role `100`; roles `110-140` are pending placeholders.
- Next useful action: run advanced reviewer roles `110-140` on the final candidate, or commit/push the infrastructure cleanup.

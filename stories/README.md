# Public story template

This public folder must not contain real story projects.

Use only:

- `stories/_template/` as a copy source for new private stories.

Real story folders belong in:

```text
private/stories/<story-slug>/
```

The root `.gitignore` prevents accidental commits of `stories/<story-slug>/`.

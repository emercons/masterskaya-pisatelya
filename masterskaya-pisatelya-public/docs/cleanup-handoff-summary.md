# Cleanup handoff summary

Use this compact context for the next session:

- Repository is now centered on `prompts/00-*` plus canonical bilingual role prompts `003-150`, with `003` for revision routing.
- Legacy prompt files, old prompt branch expectations, old helper folders, and old story template files were removed or replaced.
- `README.md`, `AGENTS.md`, `docs/story-project-structure.md`, `prompts/00-workflow.md`, and `docs/role-map.md` are aligned to bilingual prompt naming.
- `stories/_template` is now canonical and includes handoff placeholders for roles `003-150`, plus `06-agent-queue/agent-queue.md`.
- Concrete stories now belong under `masterskaya-pisatelya-PRIVATE/stories/<story-slug>/`.
- Existing private story workspace has completed content through role `150`; advanced reviewer roles `110-140` are filled.
- Full-draft exports should now use versioned filenames such as `full-draft-v6-05-07.md`.
- Next useful action: human author review of the final export, or commit/push the latest infrastructure updates.
